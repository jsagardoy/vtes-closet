import { CryptType } from '../../../../types/crypt_type';
import './ModalCrypt';

interface CryptProp {
  crypt: CryptType;
  show: boolean;
}

const ModalCryp = (prop: CryptProp) => {
};

export default ModalCryp;
 